# ARM64 Installer Notes

- For `TARGET_ARCH=aarch64`, install UEFI ARM64 bootloader stubs.
- Include DTB and vendor firmware guidance for target hardware.
- Prefer prebuilt kernel images for test runs to avoid long native builds.
